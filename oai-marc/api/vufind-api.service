[Unit]
Description=Vufind REST API
After=network.target auditd.service

[Service]
Type=simple
ExecStart=/usr/local/bin/api/vufind-api > /var/log/vufind-api.log 2>&1 &
WorkingDirectory=/usr/local/bin/api
Restart=on-failure

[Install]
WantedBy=multi-user.target

